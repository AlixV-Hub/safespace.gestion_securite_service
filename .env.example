# Configuration exemple - À copier dans .env pour personnaliser

# Profil Spring Boot (dev, test, prod)
SPRING_PROFILE=prod

# Base de données
DB_USER=safeuser
DB_PASSWORD=change_me_in_production

# Note: Ne jamais commiter le fichier .env !